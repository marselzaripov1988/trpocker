apiVersion: v1
kind: Service
metadata:
  name: truholdem-backend
  namespace: truholdem-pyramid
spec:
  type: ClusterIP
  selector:
    app: truholdem-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
